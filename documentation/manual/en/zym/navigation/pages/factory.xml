<?xml version="1.0" encoding="UTF-8"?>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink"
    xmlns:xi="http://www.w3.org/2001/XInclude" xml:lang="en" xml:id="zym.navigation.pages.factory" version="5.0">
    <title>Creating pages using the page factory</title>
    
    <para>
        All pages (also custom classes), can be created using the page
        factory, <code>Zym_Navigation_Page::factory()</code>. The factory
        can take an array with options, or a 
        <classname>Zend_Config</classname> object. Each key in the 
        array/config corresponds to a page option, as seen in the 
        section on <link xlink:href="#zym.navigation.pages">Pages</link>. 
        If the option <code>uri</code> is given and no MVC options are 
        given (<code>action, controller, module, route</code>), an URI 
        page will be created. If any of the MVC options are given, an 
        MVC page will be created.
    </para>
    
    <para>
        If <code>type</code> is given, the factory will assume the value to
        be the name of the class that should be created. If the value is
        <code>mvc</code> or <code>uri</code> and MVC/URI page will be created.
    </para>

    <example xml:id="zym.navigation.pages.factory.mvc">
        <title>Creating an MVC page using the page factory</title>
        
        <programlisting role="php"><xi:include href="factory/example/factory.mvc.php" parse="text" /></programlisting>
    </example>

    <example xml:id="zym.navigation.pages.factory.uri">
        <title>Creating a URI page using the page factory</title>
        
        <programlisting role="php"><xi:include href="factory/example/factory.uri.php" parse="text" /></programlisting>
    </example>

    <example xml:id="zym.navigation.pages.factory.custom">
        <title>Creating a custom page type using the page factory</title>
    
     <para>
         To create a custom page type using the factory, use the option 
         <code>type</code> to specify a class name to instantiate.
     </para>
        
        <programlisting role="php"><xi:include href="factory/example/factory.custom.php" parse="text" /></programlisting>
    </example>
</section>