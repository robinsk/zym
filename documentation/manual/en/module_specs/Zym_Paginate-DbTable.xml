<sect1 id="zym.paginate.dbtable">
    <title>Zym_Paginate_DbTable</title>

    <para>
        The paginator is likely to return a wrong row count when trying to paginate (complex) joined queries. This is an issue that might not get addressed in the future.
        Consider creating a View for those complex queries and select the desired data from there.
    </para>

    <programlisting role="php"><![CDATA[
class Foo_Table extends Zend_Db_Table_Abstract
{
    public function getCoolStuffPaginator($perpage = 10)
    {
        $select = $this->select()->where('status=?', 'cool');

        $paginate = new Zym_Paginate_DbTable($this, $select);
        $paginate->setRowLimit($perpage);

        return $paginate;
    }
}
]]></programlisting>
</sect1>